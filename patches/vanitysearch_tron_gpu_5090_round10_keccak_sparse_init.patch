diff --git a/GPU/GPUTron.h b/GPU/GPUTron.h
index c67f6d6..a5a2c5d 100644
--- a/GPU/GPUTron.h
+++ b/GPU/GPUTron.h
@@ -267,12 +267,31 @@ __device__ __forceinline__ uint8_t tron_keccak_state_byte(
   return static_cast<uint8_t>((state[pos >> 3] >> (8 * (pos & 7))) & 0xff);
 }
 
+__device__ __forceinline__ void tron_keccak_fixed64_tail(uint64_t state[25]) {
+  state[8] = 0x01ULL;
+  state[9] = 0;
+  state[10] = 0;
+  state[11] = 0;
+  state[12] = 0;
+  state[13] = 0;
+  state[14] = 0;
+  state[15] = 0;
+  state[16] = 0x8000000000000000ULL;
+  state[17] = 0;
+  state[18] = 0;
+  state[19] = 0;
+  state[20] = 0;
+  state[21] = 0;
+  state[22] = 0;
+  state[23] = 0;
+  state[24] = 0;
+}
+
 __device__ __noinline__ void tron_payload21_from_xy(
     const uint64_t *x,
     const uint64_t *y,
     uint8_t payload21[21]) {
   uint64_t state[25];
-  for (int i = 0; i < 25; ++i) state[i] = 0;
 
   const uint32_t *x32 = reinterpret_cast<const uint32_t *>(x);
   const uint32_t *y32 = reinterpret_cast<const uint32_t *>(y);
@@ -290,8 +309,7 @@ __device__ __noinline__ void tron_payload21_from_xy(
         (static_cast<uint64_t>(tron_bswap32(y32[lo])) << 32);
   }
 
-  state[8] = 0x01ULL;
-  state[16] = 0x8000000000000000ULL;
+  tron_keccak_fixed64_tail(state);
   tron_keccak_f1600(state);
 
   payload21[0] = 0x41;
@@ -359,7 +377,6 @@ __device__ __noinline__ bool tron_check_xy_suffix5(
     bool force_accept,
     uint8_t payload21[21]) {
   uint64_t state[25];
-  for (int i = 0; i < 25; ++i) state[i] = 0;
 
   const uint32_t *x32 = reinterpret_cast<const uint32_t *>(x);
   const uint32_t *y32 = reinterpret_cast<const uint32_t *>(y);
@@ -377,8 +394,7 @@ __device__ __noinline__ bool tron_check_xy_suffix5(
         (static_cast<uint64_t>(tron_bswap32(y32[lo])) << 32);
   }
 
-  state[8] = 0x01ULL;
-  state[16] = 0x8000000000000000ULL;
+  tron_keccak_fixed64_tail(state);
   tron_keccak_f1600(state);
 
   const uint32_t d0 = tron_keccak_state_be32_aligned4(state, 3);
